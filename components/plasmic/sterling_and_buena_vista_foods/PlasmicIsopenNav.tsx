/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
/* prettier-ignore-start */

/** @jsxRuntime classic */
/** @jsx createPlasmicElementProxy */
/** @jsxFrag React.Fragment */

// This class is auto-generated by Plasmic; please do not edit!
// Plasmic Project: uyaK17nhz8WhGjYZfKjMhX
// Component: gGkWrZU4DY6g

import * as React from "react";

import Head from "next/head";
import Link, { LinkProps } from "next/link";
import { useRouter } from "next/router";

import {
  Flex as Flex__,
  MultiChoiceArg,
  PlasmicDataSourceContextProvider as PlasmicDataSourceContextProvider__,
  PlasmicIcon as PlasmicIcon__,
  PlasmicImg as PlasmicImg__,
  PlasmicLink as PlasmicLink__,
  PlasmicPageGuard as PlasmicPageGuard__,
  SingleBooleanChoiceArg,
  SingleChoiceArg,
  Stack as Stack__,
  StrictProps,
  Trans as Trans__,
  classNames,
  createPlasmicElementProxy,
  deriveRenderOpts,
  ensureGlobalVariants,
  generateOnMutateForSpec,
  generateStateOnChangeProp,
  generateStateOnChangePropForCodeComponents,
  generateStateValueProp,
  get as $stateGet,
  hasVariant,
  initializeCodeComponentStates,
  initializePlasmicStates,
  makeFragment,
  omit,
  pick,
  renderPlasmicSlot,
  set as $stateSet,
  useCurrentUser,
  useDollarState,
  usePlasmicTranslator,
  useTrigger,
  wrapWithClassName
} from "@plasmicapp/react-web";
import {
  DataCtxReader as DataCtxReader__,
  useDataEnv,
  useGlobalActions
} from "@plasmicapp/react-web/lib/host";

import {
  executePlasmicDataOp,
  usePlasmicDataOp,
  usePlasmicInvalidate
} from "@plasmicapp/react-web/lib/data-sources";

import { Fetcher } from "@plasmicapp/react-web/lib/data-sources";
import { _useGlobalVariants } from "./plasmic"; // plasmic-import: uyaK17nhz8WhGjYZfKjMhX/projectModule
import { _useStyleTokens } from "./PlasmicStyleTokensProvider"; // plasmic-import: uyaK17nhz8WhGjYZfKjMhX/styleTokensProvider

import "@plasmicapp/react-web/lib/plasmic.css";

import sty from "./PlasmicIsopenNav.module.css"; // plasmic-import: gGkWrZU4DY6g/css

import FacebookSvgrepoCom1SvgIcon from "./icons/PlasmicIcon__FacebookSvgrepoCom1Svg"; // plasmic-import: tl3LiQrNP2Nf/icon
import InstagramSvgrepoCom5SvgIcon from "./icons/PlasmicIcon__InstagramSvgrepoCom5Svg"; // plasmic-import: 4-9LDmZK4HPz/icon
import Linkedin161SvgrepoCom2SvgIcon from "./icons/PlasmicIcon__Linkedin161SvgrepoCom2Svg"; // plasmic-import: AIRz8ScytNC6/icon

createPlasmicElementProxy;

export type PlasmicIsopenNav__VariantMembers = {
  animateImage: "animateImage";
};
export type PlasmicIsopenNav__VariantsArgs = {
  animateImage?: SingleBooleanChoiceArg<"animateImage">;
};
type VariantPropType = keyof PlasmicIsopenNav__VariantsArgs;
export const PlasmicIsopenNav__VariantProps = new Array<VariantPropType>(
  "animateImage"
);

export type PlasmicIsopenNav__ArgsType = {};
type ArgPropType = keyof PlasmicIsopenNav__ArgsType;
export const PlasmicIsopenNav__ArgProps = new Array<ArgPropType>();

export type PlasmicIsopenNav__OverridesType = {
  isopenNav?: Flex__<"div">;
  img?: Flex__<typeof PlasmicImg__>;
  _1?: Flex__<"a"> & Partial<LinkProps>;
  _8?: Flex__<"a"> & Partial<LinkProps>;
  _9?: Flex__<"a"> & Partial<LinkProps>;
  _10?: Flex__<"a"> & Partial<LinkProps>;
  _11?: Flex__<"a"> & Partial<LinkProps>;
  _12?: Flex__<"a"> & Partial<LinkProps>;
  _13?: Flex__<"a"> & Partial<LinkProps>;
};

export interface DefaultIsopenNavProps {
  animateImage?: SingleBooleanChoiceArg<"animateImage">;
  className?: string;
}

const $$ = {};

function useNextRouter() {
  try {
    return useRouter();
  } catch {}
  return undefined;
}

function PlasmicIsopenNav__RenderFunc(props: {
  variants: PlasmicIsopenNav__VariantsArgs;
  args: PlasmicIsopenNav__ArgsType;
  overrides: PlasmicIsopenNav__OverridesType;
  forNode?: string;
}) {
  const { variants, overrides, forNode } = props;

  const args = React.useMemo(
    () =>
      Object.assign(
        {},
        Object.fromEntries(
          Object.entries(props.args).filter(([_, v]) => v !== undefined)
        )
      ),
    [props.args]
  );

  const $props = {
    ...args,
    ...variants
  };

  const __nextRouter = useNextRouter();

  const $ctx = useDataEnv?.() || {};
  const refsRef = React.useRef({});
  const $refs = refsRef.current;

  const stateSpecs: Parameters<typeof useDollarState>[0] = React.useMemo(
    () => [
      {
        path: "hoverImage",
        type: "private",
        variableType: "text",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) =>
          (() => {
            try {
              return $queries.headerComponent.data.response.data.component
                .components.image.node.mediaItemUrl;
            } catch (e) {
              if (
                e instanceof TypeError ||
                e?.plasmicType === "PlasmicUndefinedDataError"
              ) {
                return undefined;
              }
              throw e;
            }
          })()
      },
      {
        path: "isHovering",
        type: "private",
        variableType: "boolean",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) => false
      },
      {
        path: "animateImage",
        type: "private",
        variableType: "variant",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) =>
          $props.animateImage
      }
    ],
    [$props, $ctx, $refs]
  );

  const globalVariants = _useGlobalVariants();

  let [$queries, setDollarQueries] = React.useState<
    Record<string, ReturnType<typeof usePlasmicDataOp>>
  >({});
  const $state = useDollarState(stateSpecs, {
    $props,
    $ctx,
    $queries: $queries,
    $q: {},
    $refs
  });

  const new$Queries: Record<string, ReturnType<typeof usePlasmicDataOp>> = {
    headerComponent: usePlasmicDataOp(() => {
      return {
        sourceId: "oysuYFSoAdb9rhuPV27pRM",
        opId: "1e3e083e-48b7-4106-9227-c37010d548b7",
        userArgs: {},
        cacheKey: `plasmic.$.1e3e083e-48b7-4106-9227-c37010d548b7.$.`,
        invalidatedKeys: null,
        roleId: null
      };
    })
  };
  if (Object.keys(new$Queries).some(k => new$Queries[k] !== $queries[k])) {
    setDollarQueries(new$Queries);

    $queries = new$Queries;
  }

  const styleTokensClassNames = _useStyleTokens();

  return (
    <div
      data-plasmic-name={"isopenNav"}
      data-plasmic-override={overrides.isopenNav}
      data-plasmic-root={true}
      data-plasmic-for-node={forNode}
      className={classNames(
        "all",
        "root_reset_uyaK17nhz8WhGjYZfKjMhX",
        "plasmic_default_styles",
        "plasmic_mixins",
        styleTokensClassNames,
        sty.isopenNav,
        {
          [sty.isopenNavanimateImage]: hasVariant(
            $state,
            "animateImage",
            "animateImage"
          )
        }
      )}
    >
      <div
        className={classNames("all", sty.freeBox__n5Xy, {
          [sty.freeBoxanimateImage__n5XySrhyo]: hasVariant(
            $state,
            "animateImage",
            "animateImage"
          )
        })}
      >
        <PlasmicImg__
          data-plasmic-name={"img"}
          data-plasmic-override={overrides.img}
          alt={""}
          className={classNames(sty.img)}
          displayHeight={"auto"}
          displayMaxHeight={"none"}
          displayMaxWidth={"100%"}
          displayMinHeight={"0"}
          displayMinWidth={"0"}
          displayWidth={
            hasVariant(globalVariants, "screen", "smallTablet")
              ? "350px"
              : "auto"
          }
          loading={"lazy"}
          src={(() => {
            try {
              return $state.hoverImage;
            } catch (e) {
              if (
                e instanceof TypeError ||
                e?.plasmicType === "PlasmicUndefinedDataError"
              ) {
                return {
                  src: "/plasmic/sterling_and_buena_vista_foods/images/applepiePng.png",
                  fullWidth: 700,
                  fullHeight: 483,
                  aspectRatio: undefined
                };
              }
              throw e;
            }
          })()}
        />
      </div>
      <div className={classNames("all", sty.freeBox__cek1N)}>
        <div className={classNames("all", sty.freeBox__mc2Vk)}>
          <PlasmicLink__
            data-plasmic-name={"_1"}
            data-plasmic-override={overrides._1}
            className={classNames("all", "a", "a__uyaK1", sty._1)}
            component={Link}
            href={`/about-us`}
            legacyBehavior={false}
            onMouseEnter={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.menu[0].image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }

              $steps["updateHoverImage2"] = true
                ? (() => {
                    const actionArgs = {
                      vgroup: "animateImage",
                      operation: 2,
                      value: "animateImage"
                    };
                    return (({ vgroup, value }) => {
                      if (typeof value === "string") {
                        value = [value];
                      }

                      const oldValue = $stateGet($state, vgroup);
                      $stateSet($state, vgroup, !oldValue);
                      return !oldValue;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage2"] != null &&
                typeof $steps["updateHoverImage2"] === "object" &&
                typeof $steps["updateHoverImage2"].then === "function"
              ) {
                $steps["updateHoverImage2"] = await $steps["updateHoverImage2"];
              }
            }}
            onMouseLeave={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }
            }}
            platform={"nextjs"}
          >
            <h3
              className={classNames(
                "all",
                "h3",
                "h3__uyaK1",
                "__wab_text",
                sty.h3__ftqeH
              )}
              onMouseEnter={async event => {
                const $steps = {};

                $steps["updateVariant"] = true
                  ? (() => {
                      const actionArgs = {};
                      return (({ vgroup, value }) => {
                        if (typeof value === "string") {
                          value = [value];
                        }
                        undefined;
                      })?.apply(null, [actionArgs]);
                    })()
                  : undefined;
                if (
                  $steps["updateVariant"] != null &&
                  typeof $steps["updateVariant"] === "object" &&
                  typeof $steps["updateVariant"].then === "function"
                ) {
                  $steps["updateVariant"] = await $steps["updateVariant"];
                }
              }}
            >
              <React.Fragment>
                {(() => {
                  try {
                    return $queries.headerComponent.data.response.data.component
                      .components.menu[0].menuitem;
                  } catch (e) {
                    if (
                      e instanceof TypeError ||
                      e?.plasmicType === "PlasmicUndefinedDataError"
                    ) {
                      return "";
                    }
                    throw e;
                  }
                })()}
              </React.Fragment>
            </h3>
          </PlasmicLink__>
          <PlasmicLink__
            data-plasmic-name={"_8"}
            data-plasmic-override={overrides._8}
            className={classNames("all", "a", "a__uyaK1", sty._8)}
            component={Link}
            href={`/capabilities`}
            legacyBehavior={false}
            onMouseEnter={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.menu[0].image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }

              $steps["updateHoverImage2"] = true
                ? (() => {
                    const actionArgs = {
                      vgroup: "animateImage",
                      operation: 2,
                      value: "animateImage"
                    };
                    return (({ vgroup, value }) => {
                      if (typeof value === "string") {
                        value = [value];
                      }

                      const oldValue = $stateGet($state, vgroup);
                      $stateSet($state, vgroup, !oldValue);
                      return !oldValue;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage2"] != null &&
                typeof $steps["updateHoverImage2"] === "object" &&
                typeof $steps["updateHoverImage2"].then === "function"
              ) {
                $steps["updateHoverImage2"] = await $steps["updateHoverImage2"];
              }
            }}
            onMouseLeave={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }
            }}
            platform={"nextjs"}
          >
            <h3
              className={classNames(
                "all",
                "h3",
                "h3__uyaK1",
                "__wab_text",
                sty.h3__bb2I
              )}
            >
              <React.Fragment>
                {(() => {
                  try {
                    return $queries.headerComponent.data.response.data.component
                      .components.menu[1].menuitem;
                  } catch (e) {
                    if (
                      e instanceof TypeError ||
                      e?.plasmicType === "PlasmicUndefinedDataError"
                    ) {
                      return "About  Us";
                    }
                    throw e;
                  }
                })()}
              </React.Fragment>
            </h3>
          </PlasmicLink__>
          <PlasmicLink__
            data-plasmic-name={"_9"}
            data-plasmic-override={overrides._9}
            className={classNames("all", "a", "a__uyaK1", sty._9)}
            component={Link}
            href={`/markets`}
            legacyBehavior={false}
            onMouseEnter={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.menu[0].image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }

              $steps["updateHoverImage2"] = true
                ? (() => {
                    const actionArgs = {
                      vgroup: "animateImage",
                      operation: 2,
                      value: "animateImage"
                    };
                    return (({ vgroup, value }) => {
                      if (typeof value === "string") {
                        value = [value];
                      }

                      const oldValue = $stateGet($state, vgroup);
                      $stateSet($state, vgroup, !oldValue);
                      return !oldValue;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage2"] != null &&
                typeof $steps["updateHoverImage2"] === "object" &&
                typeof $steps["updateHoverImage2"].then === "function"
              ) {
                $steps["updateHoverImage2"] = await $steps["updateHoverImage2"];
              }
            }}
            onMouseLeave={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }
            }}
            platform={"nextjs"}
          >
            <h3
              className={classNames(
                "all",
                "h3",
                "h3__uyaK1",
                "__wab_text",
                sty.h3___3WhdV
              )}
            >
              <React.Fragment>
                {(() => {
                  try {
                    return $queries.headerComponent.data.response.data.component
                      .components.menu[2].menuitem;
                  } catch (e) {
                    if (
                      e instanceof TypeError ||
                      e?.plasmicType === "PlasmicUndefinedDataError"
                    ) {
                      return "About  Us";
                    }
                    throw e;
                  }
                })()}
              </React.Fragment>
            </h3>
          </PlasmicLink__>
          <PlasmicLink__
            data-plasmic-name={"_10"}
            data-plasmic-override={overrides._10}
            className={classNames("all", "a", "a__uyaK1", sty._10)}
            component={Link}
            href={`/our-facilities`}
            legacyBehavior={false}
            onMouseEnter={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.menu[0].image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }

              $steps["updateHoverImage2"] = true
                ? (() => {
                    const actionArgs = {
                      vgroup: "animateImage",
                      operation: 2,
                      value: "animateImage"
                    };
                    return (({ vgroup, value }) => {
                      if (typeof value === "string") {
                        value = [value];
                      }

                      const oldValue = $stateGet($state, vgroup);
                      $stateSet($state, vgroup, !oldValue);
                      return !oldValue;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage2"] != null &&
                typeof $steps["updateHoverImage2"] === "object" &&
                typeof $steps["updateHoverImage2"].then === "function"
              ) {
                $steps["updateHoverImage2"] = await $steps["updateHoverImage2"];
              }
            }}
            onMouseLeave={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }
            }}
            platform={"nextjs"}
          >
            <h3
              className={classNames(
                "all",
                "h3",
                "h3__uyaK1",
                "__wab_text",
                sty.h3__nQoU3
              )}
            >
              <React.Fragment>
                {(() => {
                  try {
                    return $queries.headerComponent.data.response.data.component
                      .components.menu[3].menuitem;
                  } catch (e) {
                    if (
                      e instanceof TypeError ||
                      e?.plasmicType === "PlasmicUndefinedDataError"
                    ) {
                      return "About  Us";
                    }
                    throw e;
                  }
                })()}
              </React.Fragment>
            </h3>
          </PlasmicLink__>
          <PlasmicLink__
            data-plasmic-name={"_11"}
            data-plasmic-override={overrides._11}
            className={classNames("all", "a", "a__uyaK1", sty._11)}
            component={Link}
            href={`/quality-safety`}
            legacyBehavior={false}
            onMouseEnter={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.menu[0].image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }

              $steps["updateHoverImage2"] = true
                ? (() => {
                    const actionArgs = {
                      vgroup: "animateImage",
                      operation: 2,
                      value: "animateImage"
                    };
                    return (({ vgroup, value }) => {
                      if (typeof value === "string") {
                        value = [value];
                      }

                      const oldValue = $stateGet($state, vgroup);
                      $stateSet($state, vgroup, !oldValue);
                      return !oldValue;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage2"] != null &&
                typeof $steps["updateHoverImage2"] === "object" &&
                typeof $steps["updateHoverImage2"].then === "function"
              ) {
                $steps["updateHoverImage2"] = await $steps["updateHoverImage2"];
              }
            }}
            onMouseLeave={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }
            }}
            platform={"nextjs"}
          >
            <h3
              className={classNames(
                "all",
                "h3",
                "h3__uyaK1",
                "__wab_text",
                sty.h3__pa7Rv
              )}
            >
              <React.Fragment>
                {(() => {
                  try {
                    return $queries.headerComponent.data.response.data.component
                      .components.menu[4].menuitem;
                  } catch (e) {
                    if (
                      e instanceof TypeError ||
                      e?.plasmicType === "PlasmicUndefinedDataError"
                    ) {
                      return "About  Us";
                    }
                    throw e;
                  }
                })()}
              </React.Fragment>
            </h3>
          </PlasmicLink__>
          <PlasmicLink__
            data-plasmic-name={"_12"}
            data-plasmic-override={overrides._12}
            className={classNames("all", "a", "a__uyaK1", sty._12)}
            component={Link}
            href={`/careers`}
            legacyBehavior={false}
            onMouseEnter={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.menu[0].image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }

              $steps["updateHoverImage2"] = true
                ? (() => {
                    const actionArgs = {
                      vgroup: "animateImage",
                      operation: 2,
                      value: "animateImage"
                    };
                    return (({ vgroup, value }) => {
                      if (typeof value === "string") {
                        value = [value];
                      }

                      const oldValue = $stateGet($state, vgroup);
                      $stateSet($state, vgroup, !oldValue);
                      return !oldValue;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage2"] != null &&
                typeof $steps["updateHoverImage2"] === "object" &&
                typeof $steps["updateHoverImage2"].then === "function"
              ) {
                $steps["updateHoverImage2"] = await $steps["updateHoverImage2"];
              }
            }}
            onMouseLeave={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }
            }}
            platform={"nextjs"}
          >
            <h3
              className={classNames(
                "all",
                "h3",
                "h3__uyaK1",
                "__wab_text",
                sty.h3___8F302
              )}
            >
              <React.Fragment>
                {(() => {
                  try {
                    return $queries.headerComponent.data.response.data.component
                      .components.menu[5].menuitem;
                  } catch (e) {
                    if (
                      e instanceof TypeError ||
                      e?.plasmicType === "PlasmicUndefinedDataError"
                    ) {
                      return "About  Us";
                    }
                    throw e;
                  }
                })()}
              </React.Fragment>
            </h3>
          </PlasmicLink__>
          <PlasmicLink__
            data-plasmic-name={"_13"}
            data-plasmic-override={overrides._13}
            className={classNames("all", "a", "a__uyaK1", sty._13)}
            component={Link}
            href={`/contact-us`}
            legacyBehavior={false}
            onMouseEnter={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.menu[0].image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }

              $steps["updateHoverImage2"] = true
                ? (() => {
                    const actionArgs = {
                      vgroup: "animateImage",
                      operation: 2,
                      value: "animateImage"
                    };
                    return (({ vgroup, value }) => {
                      if (typeof value === "string") {
                        value = [value];
                      }

                      const oldValue = $stateGet($state, vgroup);
                      $stateSet($state, vgroup, !oldValue);
                      return !oldValue;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage2"] != null &&
                typeof $steps["updateHoverImage2"] === "object" &&
                typeof $steps["updateHoverImage2"].then === "function"
              ) {
                $steps["updateHoverImage2"] = await $steps["updateHoverImage2"];
              }
            }}
            onMouseLeave={async event => {
              const $steps = {};

              $steps["updateHoverImage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["hoverImage"]
                      },
                      operation: 0,
                      value:
                        $queries.headerComponent.data.response.data.component
                          .components.image.node.mediaItemUrl
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updateHoverImage"] != null &&
                typeof $steps["updateHoverImage"] === "object" &&
                typeof $steps["updateHoverImage"].then === "function"
              ) {
                $steps["updateHoverImage"] = await $steps["updateHoverImage"];
              }
            }}
            platform={"nextjs"}
          >
            <h3
              className={classNames(
                "all",
                "h3",
                "h3__uyaK1",
                "__wab_text",
                sty.h3__bDjXc
              )}
            >
              <React.Fragment>
                {(() => {
                  try {
                    return $queries.headerComponent.data.response.data.component
                      .components.menu[6].menuitem;
                  } catch (e) {
                    if (
                      e instanceof TypeError ||
                      e?.plasmicType === "PlasmicUndefinedDataError"
                    ) {
                      return "About  Us";
                    }
                    throw e;
                  }
                })()}
              </React.Fragment>
            </h3>
          </PlasmicLink__>
        </div>
        <div className={classNames("all", sty.freeBox__wqMwo)}>
          <PlasmicLink__
            className={classNames("all", "a", "a__uyaK1", sty.link___04GZ)}
            component={Link}
            href={
              $queries.headerComponent.data.response.data.component.components
                .socialMedia[0].urlSocial
            }
            legacyBehavior={false}
            platform={"nextjs"}
          >
            <FacebookSvgrepoCom1SvgIcon
              className={classNames("all", sty.svg___90NHv)}
              role={"img"}
            />
          </PlasmicLink__>
          <PlasmicLink__
            className={classNames("all", "a", "a__uyaK1", sty.link__cjEy)}
            component={Link}
            href={
              $queries.headerComponent.data.response.data.component.components
                .socialMedia[1].urlSocial
            }
            legacyBehavior={false}
            platform={"nextjs"}
          >
            <InstagramSvgrepoCom5SvgIcon
              className={classNames("all", sty.svg___4UZcw)}
              role={"img"}
            />
          </PlasmicLink__>
          <PlasmicLink__
            className={classNames("all", "a", "a__uyaK1", sty.link__pUfms)}
            component={Link}
            href={
              $queries.headerComponent.data.response.data.component.components
                .socialMedia[2].urlSocial
            }
            legacyBehavior={false}
            platform={"nextjs"}
          >
            <Linkedin161SvgrepoCom2SvgIcon
              className={classNames("all", sty.svg__lufdY)}
              role={"img"}
            />
          </PlasmicLink__>
        </div>
      </div>
    </div>
  ) as React.ReactElement | null;
}

const PlasmicDescendants = {
  isopenNav: ["isopenNav", "img", "_1", "_8", "_9", "_10", "_11", "_12", "_13"],
  img: ["img"],
  _1: ["_1"],
  _8: ["_8"],
  _9: ["_9"],
  _10: ["_10"],
  _11: ["_11"],
  _12: ["_12"],
  _13: ["_13"]
} as const;
type NodeNameType = keyof typeof PlasmicDescendants;
type DescendantsType<T extends NodeNameType> =
  (typeof PlasmicDescendants)[T][number];
type NodeDefaultElementType = {
  isopenNav: "div";
  img: typeof PlasmicImg__;
  _1: "a";
  _8: "a";
  _9: "a";
  _10: "a";
  _11: "a";
  _12: "a";
  _13: "a";
};

type ReservedPropsType = "variants" | "args" | "overrides";
type NodeOverridesType<T extends NodeNameType> = Pick<
  PlasmicIsopenNav__OverridesType,
  DescendantsType<T>
>;
type NodeComponentProps<T extends NodeNameType> =
  // Explicitly specify variants, args, and overrides as objects
  {
    variants?: PlasmicIsopenNav__VariantsArgs;
    args?: PlasmicIsopenNav__ArgsType;
    overrides?: NodeOverridesType<T>;
  } &
    // Specify variants directly as props
    Omit<PlasmicIsopenNav__VariantsArgs, ReservedPropsType> &
    // Specify args directly as props
    Omit<PlasmicIsopenNav__ArgsType, ReservedPropsType> &
    // Specify overrides for each element directly as props
    Omit<
      NodeOverridesType<T>,
      ReservedPropsType | VariantPropType | ArgPropType
    > &
    // Specify props for the root element
    Omit<
      Partial<React.ComponentProps<NodeDefaultElementType[T]>>,
      ReservedPropsType | VariantPropType | ArgPropType | DescendantsType<T>
    >;

function makeNodeComponent<NodeName extends NodeNameType>(nodeName: NodeName) {
  type PropsType = NodeComponentProps<NodeName> & { key?: React.Key };
  const func = function <T extends PropsType>(
    props: T & StrictProps<T, PropsType>
  ) {
    const { variants, args, overrides } = React.useMemo(
      () =>
        deriveRenderOpts(props, {
          name: nodeName,
          descendantNames: PlasmicDescendants[nodeName],
          internalArgPropNames: PlasmicIsopenNav__ArgProps,
          internalVariantPropNames: PlasmicIsopenNav__VariantProps
        }),
      [props, nodeName]
    );
    return PlasmicIsopenNav__RenderFunc({
      variants,
      args,
      overrides,
      forNode: nodeName
    });
  };
  if (nodeName === "isopenNav") {
    func.displayName = "PlasmicIsopenNav";
  } else {
    func.displayName = `PlasmicIsopenNav.${nodeName}`;
  }
  return func;
}

export const PlasmicIsopenNav = Object.assign(
  // Top-level PlasmicIsopenNav renders the root element
  makeNodeComponent("isopenNav"),
  {
    // Helper components rendering sub-elements
    img: makeNodeComponent("img"),
    _1: makeNodeComponent("_1"),
    _8: makeNodeComponent("_8"),
    _9: makeNodeComponent("_9"),
    _10: makeNodeComponent("_10"),
    _11: makeNodeComponent("_11"),
    _12: makeNodeComponent("_12"),
    _13: makeNodeComponent("_13"),

    // Metadata about props expected for PlasmicIsopenNav
    internalVariantProps: PlasmicIsopenNav__VariantProps,
    internalArgProps: PlasmicIsopenNav__ArgProps
  }
);

export default PlasmicIsopenNav;
/* prettier-ignore-end */
